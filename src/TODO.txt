ChangeLog
=========

2003-11-15 - dadler

* with MinGW version 3.0.1 pixmap loading does work

* project.mk , win32.mk and x11.mk in src/build changed
  now a single variable MODS will extend.

* MinGW build system changed.
  rgl.dll now contains an R compilant Resource information
  generated by R perl script

* bug fix: R 1.8.0/win32 does not detach packages when quit
  it is safe now to call rgl_quit and lib_quit multiple times
  win32lib.cpp: added dllmain that calls rgl_quit on process exit  

* added core support for devcpp IDE

CHECK
=====

- unix build system
- R 1.8.0 shutdown behaviour eventually using a C++ static object that
  calls rgl_quit

BUGS
====

COSMETICS
=========

- package vignette

- mingw: two build warnings
  
  g++ -Wall -pedantic -O2 -fomit-frame-pointer -fno-exceptions -fno-rtti -Izlib -I
  lpng124 -DHAVE_PNG_H  -c -o win32lib.o win32lib.cpp
  In file included from win32lib.cpp:13:
  win32gui.h:17: warning: `class gui::Win32GUIFactory' has virtual functions but
  non-virtual destructor
  g++ -Wall -pedantic -O2 -fomit-frame-pointer -fno-exceptions -fno-rtti -Izlib -I
  lpng124 -DHAVE_PNG_H  -c -o win32gui.o win32gui.cpp
  In file included from win32gui.cpp:6:
  win32gui.h:17: warning: `class gui::Win32GUIFactory' has virtual functions but
  non-virtual destructor

- x11: rgl.snapshot requires window to be opened
  fix with offscreen pixmap rendering and support for output parameters

- x11: ChooseVisual for depth,alpha and 15,16 bit failed on dadler's machine

- win32: window icon

- verify docs added:

  BUILD/mingw requirements:
  - R Version 1.8.0
  - ActiveState Perl
  - cygwin's autoconf/automake for zlib/libpng
  - rtools
  - HTML Help Workshop

  path order:
  - ActiveState Perl BEFORE cygwin's perl
  - MinGW BEFORE cygwin's gcc

- Maintainer Build:
  - change to directory root-directory containing rgl

- current mingw installation requires setup mingw
  - removed to be the default mechanism
  - make vc build to be optional
  - hhc to be optional ?! -> pixmap requires these so we make it as a rule

- pure win32 mingw/rtools build fails using x11gui x11lib
  - without cygwin fails either

- rgl-maintainer manual page not accessable, to be removed!

- rgl.bbox labels

- z-sorted alpha-blended surfaces

- perspective mouse-based adjusting using 3-button emulation

- namespaces


       
TODO
====

- manual page "rgl" rewrite: toc

    overview
    devices
    scene
      shapes
      appearance
      environment
    navigation
    utils

- demo() 

scene: 
  - alpha blended shapes rendered in stack order results in visual errors
    z-distance sorted faces and back to front order

  - bbox: non-uniform scale per axis

  - allow png image size to be of arbitary size!
  
gui: 
  - capture mouse

exec:
  - embedded multi-threading framework

IDEAS
=====

R api:
  - 3d util functions

  - parametric shapes:
  
  implicit planes  e.g. y=0
           lines   e.g. x=1, y=2
           spheres e.g. x^2 + y^2 + z^2 -1 = 0

  - spheres,bg: specify sections and segments 

  - bg: sphere horizontal color range

  - material: texture magnify filter as optional using material

C++:
  - use STL
  

render:

  - multi-pass rendering (through accumulation) ... depth of field?!
